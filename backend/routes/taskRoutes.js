const r=require('express').Router(),c=require('../controllers/taskController'),activity=require('../controllers/taskActivityController'),{authenticate,authorize}=require('../middleware/authMiddleware'),a=require('../utils/asyncHandler');r.use(authenticate);r.get('/dashboard/employee',authorize('employee'),a(c.employeeDashboard));r.get('/tasks',a(c.list));r.get('/tasks/stats',a(c.stats));r.get('/tasks/:taskId/activity',a(activity.list));r.get('/tasks/:id',a(c.detail));r.post('/tasks',authorize('manager'),a(c.create));r.put('/tasks/:id',authorize('manager'),a(c.edit));r.patch('/tasks/:id/progress',authorize('employee'),a(c.progress));r.delete('/tasks/:id',authorize('manager'),a(c.remove));r.get('/employees/my-team',authorize('manager'),a(c.team));module.exports=r;
