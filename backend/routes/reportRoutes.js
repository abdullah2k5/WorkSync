const express=require('express');const c=require('../controllers/reportController');const {authenticate,authorize}=require('../middleware/authMiddleware');const asyncHandler=require('../utils/asyncHandler');const router=express.Router();router.use(authenticate,authorize('admin','manager'));router.get('/reports/summary',asyncHandler(c.summary));router.get('/reports/tasks',asyncHandler(c.tasks));router.get('/reports/leaves',asyncHandler(c.leaves));router.get('/reports/employees',asyncHandler(c.employees));router.get('/reports/charts',asyncHandler(c.chartData));router.get('/reports/meta',asyncHandler(c.meta));module.exports=router;