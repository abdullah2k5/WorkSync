const r=require('express').Router(),c=require('../controllers/leaveController'),{authenticate,authorize}=require('../middleware/authMiddleware'),a=require('../utils/asyncHandler');r.use(authenticate);r.get('/leaves',a(c.list));r.get('/leaves/summary',a(c.summary));r.get('/leaves/:id',a(c.detail));r.post('/leaves',authorize('employee'),a(c.create));r.patch('/leaves/:id/cancel',authorize('employee'),a(c.cancel));r.patch('/leaves/:id/review',authorize('manager'),a(c.review));module.exports=r;
